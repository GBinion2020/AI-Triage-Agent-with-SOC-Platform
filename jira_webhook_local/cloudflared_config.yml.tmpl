tunnel: ${CF_TUNNEL_ID}
credentials-file: ${CF_TUNNEL_CREDENTIALS_FILE}

ingress:
  - hostname: ${CF_TUNNEL_HOSTNAME}
    service: http://127.0.0.1:${FEEDBACK_RECEIVER_PORT}
  - service: http_status:404
